-- Create database if it doesn't exist
CREATE DATABASE IF NOT EXISTS smartcity CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Use the database
USE smartcity;

-- Show confirmation
SELECT 'Database smartcity created successfully!' AS message;
